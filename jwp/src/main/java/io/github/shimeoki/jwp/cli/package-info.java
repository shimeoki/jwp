/**
 * Defines the UI for the command line.
 *
 * <p>
 * This UI also acts as a standard interface to run the app in the first place,
 * so it should be used unconditionally even if another TUI/GUI is used.
 */
package io.github.shimeoki.jwp.cli;
