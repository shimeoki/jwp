/**
 * Defines the value objects for the domain repositories and entities.
 *
 * <p>
 * Value objects are essentially primitives with validation. They should be
 * immutable, so copying them by reference is preferred to save memory.
 */
package io.github.shimeoki.jwp.domain.values;
